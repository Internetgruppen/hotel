version: '2.3'
services:
  apache:
    image: internetgruppen/hotel:php-5.6-apache
    mem_limit: 128m
    environment:
      APACHE_RUN_USER: www-data
      APACHE_RUN_GROUP: www-data
    networks:
      - hotel
    ports:
    - 80/tcp

networks:
  hotel:
